<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loki.server.dao.IntentionLogDao">
  <cache eviction="LRU" type="com.loki.server.cache.RedisCache"/>
  <resultMap id="intentionLogDO" type="com.loki.server.entity.IntentionLog">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="available_amount" jdbcType="DECIMAL" property="availableAmount" />
    <result column="freeze_amount" jdbcType="DECIMAL" property="freezeAmount" />
    <result column="content" jdbcType="VARCHAR" property="content" />
    <result column="type" jdbcType="VARCHAR" property="type" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="intention_id" jdbcType="INTEGER" property="intentionId" />
    <result column="admin_id" jdbcType="INTEGER" property="adminId" />
  </resultMap>
  
  <sql id="Base_Column_List">
    id,create_time,available_amount,freeze_amount,content,type,user_id,intention_id,admin_id
  </sql>
  
  <insert id="insert" useGeneratedKeys="true" keyProperty="id"  parameterType="com.loki.server.entity.IntentionLog">
		insert into intention_log(create_time,available_amount,freeze_amount,content,type,user_id,intention_id,admin_id) 
		values(now(),#{availableAmount},#{freezeAmount},#{content},#{type},#{userId},#{intentionId},#{adminId})
	</insert>

  <update id="update" parameterType="com.loki.server.entity.IntentionLog">
  	update intention_log SET available_amount=#{availableAmount},freeze_amount=#{freezeAmount},content=#{content},type=#{type},user_id=#{userId},intention_id=#{intentionId},admin_id=#{adminId} where id=#{id}
  </update>
  
  <select id="findById" parameterType="int" resultMap="intentionLogDO">
		select <include refid="Base_Column_List" /> 
		from intention_log
		where id=#{_parameter}
  </select>
  
  <select id="findAll" resultMap="intentionLogDO">
		select <include refid="Base_Column_List" /> 
		from intention_log
  </select>
  
  <select id="findByParam" resultMap="intentionLogDO">
		select <include refid="Base_Column_List" /> 
		from intention_log
		<where>
			<if test="userId != null and userId != 0">
				user_id=#{userId,jdbcType=INTEGER}
			</if>
			<if test="intentionId != null and intentionId != 0">
				and intention_id=#{intentionId,jdbcType=INTEGER}
			</if>
			<if test="adminId != null and adminId != 0">
				and admin_id=#{adminId,jdbcType=INTEGER}
			</if>
			<if test="type != null and type != ''">
				and type=#{type,jdbcType=VARCHAR}
			</if>
		</where>
		order by id desc
  </select>
  
  <delete id="delete" parameterType="int">
		delete from intention_log where id=#{_parameter}
	</delete>
</mapper>