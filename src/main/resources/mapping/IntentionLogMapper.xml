<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.loki.server.dao.IntentionLogDao">
  <cache eviction="LRU" type="com.loki.server.cache.RedisCache"/>
  <resultMap id="intentionLogDao" type="com.loki.server.entity.IntentionLog">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="available_amount" jdbcType="DECIMAL" property="availableAmount" />
    <result column="freeze_amount" jdbcType="DECIMAL" property="freezeAmount" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="user_id" jdbcType="INTEGER" property="userId" />
    <result column="intention_id" jdbcType="INTEGER" property="intentionId" />
    <result column="admin_id" jdbcType="INTEGER" property="adminId" />
  </resultMap>
  
  <sql id="Base_Column_List">
    id,create_time,available_amount,freeze_amount,description,user_id,intention_id,admin_id
  </sql>
  
  <insert id="insert" useGeneratedKeys="true" keyProperty="id"  parameterType="com.loki.server.entity.IntentionLog">
		insert into intention_log(create_time,available_amount,freeze_amount,description,user_id,intention_id,admin_id) 
		values(now(),#{availableAmount},#{freezeAmount},#{description},#{userId},#{intentionId},#{adminId})
	</insert>

  <update id="update" parameterType="com.loki.server.entity.IntentionLog">
  	update intention_log SET available_amount=#{availableAmount},freeze_amount=#{freezeAmount},description=#{description},user_id=#{userId},intention_id=#{intentionId},admin_id=#{adminId} where id=#{id}
  </update>
  
  <select id="findById" parameterType="int" resultMap="intentionLogDao">
		select <include refid="Base_Column_List" /> 
		from intention_log
		where id=#{_parameter}
  </select>
  
  <select id="findAll" resultMap="intentionLogDao">
		select <include refid="Base_Column_List" /> 
		from intention_log
  </select>
  
  <delete id="delete" parameterType="int">
		delete from intention_log where id=#{_parameter}
	</delete>
</mapper>